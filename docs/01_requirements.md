# 01. 要件定義書（swing-copilot）

## 文書情報

| 項目 | 内容 |
|---|---|
| システム名（仮称） | swing-copilot |
| 文書種別 | 要件定義書 |
| 版数 | 1.3 |
| 作成日 | 2026-07-20 |
| 最終更新日 | 2026-07-28 |
| 関連文書 | 00_human_preparation.md（人間の下準備チェックリスト）、02_project_plan.md（プロジェクト計画書） |

---

## 背景と目的

米国株のスイング〜ポジショントレード（保有数日〜数ヶ月）を対象に、銘柄スクリーニング・分析・レポート作成までを自動化し、投資家本人の**意思決定を支援する**システムを構築する。発注（実際の売買執行）は人間がSBI証券・楽天証券等を通じて手動で行い、完全自動売買はスコープ外とする。

Part1調査の結論として、本システムは意思決定支援ツールとしては条件付きGoと判断されている。ただし「安定した利益」を実証する根拠は存在しないため、バックテストやペーパートレードで良好な成績が出た場合でも、**ペーパートレード→少額実運用という段階的な検証ゲートを必須**とする。本システムは投資助言を行わず、最終的な売買判断は常に人間が行う。

実装は最終的にClaude Codeの自律実行（`/goal`）によって行うことを前提とする。そのため、本文書の全機能要件・非機能要件には、自律実装エージェントが実装完了を判断できるよう、検証可能な受け入れ基準を付す。

利用者は日本在住の個人投資家1名（Python可）であり、1人で長期にわたり保守できることを最優先の設計方針とする。

---

## スコープ

### 対象

- S&P500構成銘柄（約500銘柄）を対象としたユニバース管理、株価・財務・ニュース・経済指標データの取得と保存（各データの取得時点・利用可能時点を保持）
- ファンダメンタル品質フィルタとテクニカルシグナルによる2段階のスクリーニング・エントリー候補抽出
- 収集したニュース・開示テキストと決定論的なスクリーニング文脈のエクスポート、Claude Codeスキルによる定性分析（ニュース解釈・開示解釈・スクリーニング評価）と、その結果の機械検証・レポート反映
- 日次結果のCLI表示、監査用Markdown保存、Discord Webhookサマリ通知（オプション機能）
- 戦略のバックテストとペーパートレードの記録・成績集計
- 上記処理を1コマンドで順次実行するオーケストレーション（ローカルでの手動実行）

### 対象外

- 証券会社APIを用いた発注の自動化（CON-01）
- 完全自動売買（最終判断・発注は常に人間が行う）
- 断定的な売買指示を含む投資助言の提供（CON-03）
- yfinanceを本番のデータ基盤として利用すること（試作限定、CON-02）

---

## 利用者・利用シナリオ

### 利用者

日本在住の個人投資家1名。Pythonでのコード読解・簡単な修正が可能。証券会社の米国株口座を保有し、発注は自身の判断で手動実施する。開発・保守は基本的に本人が単独で行う。

### 利用シナリオ（コマンド実行〜判断の流れ）

1. 利用者は1日1回、任意のタイミングでコマンド（`uv run copilot-daily`）を実行し、ユニバース更新・データ取得・スクリーニング・シグナル抽出・テキスト収集・分析入力エクスポート・レポート生成までを順次実行させる（FR-12）。この時点のレポートは定性分析欄が「未実施」の状態である。
2. 利用者はClaude Codeで`swing-daily`スキルを起動する。スキルは`analysis_input.json`を読んで定性分析（ニュース解釈・開示解釈・スクリーニング評価）を行い、`analysis_result.json`を書き出す。`uv run copilot-ingest-analysis`がその結果を機械検証し、同じレポートの定性欄だけを埋めて再描画する（FR-08）。スキルは手順1のコマンド実行自体も代行できるため、通常運用ではスキル起動1回で手順1〜2が完了する。
3. 実行完了後、CLIに候補・ファンダ分析・定性分析・銘柄ごとのverdict（見送り推奨／懸念なし）・リスク管理チェック結果が表示され、同内容のMarkdownが保存される（FR-09）。
4. 利用者はCLIの結論ファーストな一覧と銘柄別詳細を確認する（FR-05〜FR-09）。
5. 利用者はレポート内容を踏まえて自身で売買判断を行う。verdictはあくまで定性情報に基づく推奨であり、最終判断は常に人間が行う。
   - P3（ペーパートレード検証）期間中は、`copilot-decision`で判断結果（見送り／仮想エントリー等）を記録する（FR-11）。
   - 本番移行後は、実際にSBI/楽天等で手動発注する（本システムのスコープ外）。
6. 上記のコマンド実行から判断記録までは1回あたり数分〜10分程度を想定する。Discord通知（オプション機能、FR-09）を有効にしている場合は、通知を先に確認してからレポートを開いてもよい。

---

## 機能要件

### 機能要件一覧

| ID | 要件名 | 概要 |
|---|---|---|
| FR-01 | ユニバース管理 | S&P500構成銘柄リストの取得・更新・手動上書き |
| FR-02 | 株価データ取得・保存 | 日足OHLCV（調整済み）の取得・保存、DataProvider抽象化 |
| FR-03 | ファンダメンタルデータ取得 | EDGAR XBRLデータの取得・正規化・キャッシュ |
| FR-04 | スクリーニング | ファンダ品質フィルタによる母集団絞り込み（第1段） |
| FR-05 | テクニカルシグナル | トレンド＋押し目検出によるエントリー候補抽出（第2段） |
| FR-06 | リスク管理チェック | ポジションサイズ・ストップ距離・セクター集中/相関チェック |
| FR-07 | テキスト収集 | 銘柄別ニュース、新着8-K/10-Q、経済指標カレンダーの収集 |
| FR-08 | 定性分析（スキル連携） | 分析入力のエクスポート、Claude Codeスキルによる解釈、結果JSONの機械検証とレポート反映 |
| FR-09 | レポート・通知 | CLI日次ブリーフ＋監査用Markdown＋Discord Webhookサマリ通知（オプション機能） |
| FR-10 | バックテスト | 戦略の履歴検証（手数料・スリッページ込み） |
| FR-11 | ペーパートレード記録 | シグナル→判断→仮想約定→損益の記録・成績集計 |
| FR-12 | 日次バッチ | 上記処理を順次実行するオーケストレーション（1コマンドでのローカル手動実行） |

### 各要件の説明と受け入れ基準

**FR-01 ユニバース管理**
- 説明: S&P500構成銘柄リストを取得し、CSVキャッシュとDuckDBのスナップショット履歴へ保存する。明示的な`as_of`では`snapshot_date <= as_of`の最新履歴だけを使い、未来の構成銘柄を見ない。通常live実行では更新周期に従い再利用または更新し、取得元の一時障害時にも保存済み履歴を安全側に利用できる。利用者は銘柄リストを手動で上書きできる。
- 受け入れ基準:
  - 銘柄リスト取得処理を実行するコマンドが存在し、実行後に約500銘柄のティッカーリストが永続化されていることをpytestで検証できる。
  - 手動上書き用のファイルまたはコマンドが存在し、上書き後の銘柄リストが後続処理（FR-02〜FR-06）に反映されることをテストで確認できる。
  - `--as-of`実行は、直前・同日・直後の3つの履歴から`snapshot_date <= as_of`である最新だけを選び、履歴がなければ価格取得・スクリーニング前に明確なエラーで停止することを検証できる。
  - live実行は最新履歴の経過日数が`universe.refresh_interval_days`未満なら外部取得を行わず、同値以上なら1回だけ更新してCSVとDuckDBへ同じ生の構成銘柄を保存することを検証できる。
  - live更新が失敗しても既存履歴があれば、そのスナップショット日と失敗理由を警告として記録・表示し、runを`degraded`で完走する。既存履歴もなければfail-fastすることを検証できる。
  - `manual_include`/`manual_exclude`は選択済みの生スナップショットへ決定的に適用し、手動追加銘柄を履歴やCSVへ混入させないことを検証できる。

**FR-02 株価データ取得・保存**
- 説明: 各銘柄の日足OHLCVを取得し、企業行動調整済みのOHLCを共通の価格基準としてParquetに保存し、DuckDBから参照する。データ取得元は`DataProvider`インターフェースで抽象化し、試作期のyfinanceから本番移行時のEODHDへ実装を差し替えられること。プロバイダ固有の列構造や調整方式を後続処理へ漏らさない。
- 受け入れ基準:
  - `DataProvider`抽象基底クラス（またはProtocol）が定義され、少なくとも`YFinanceProvider`実装が存在し、単体テストで日足OHLCVが取得・保存できることを確認できる。
  - 既存コードを変更せずに新しい`DataProvider`実装を追加できる設計になっていることを、モック実装を用いたテストで確認できる（差し替え可能性の検証）。
  - 保存されたデータをDuckDBから再読込し、取得時と同一件数・カラム構成で取得できることをpytestで検証できる。
  - 株式分割を含むfixtureで、保存後のOHLCが同一の調整基準に揃い、シグナル・チャート・バックテストが同じ価格列を使用することをpytestで検証できる。

**FR-03 ファンダメンタルデータ取得**
- 説明: SEC EDGAR公式API（edgartools使用）から、売上・利益・キャッシュフロー・自己資本等のXBRLデータを取得し、正規化してキャッシュする。各レコードに会計期間末・提出日時・アクセッション番号を保持し、指定した評価日時点で公表済みの値だけを取得できること。EDGARのレート上限（10リクエスト/秒）とUser-Agentヘッダー必須要件を遵守する。
- 受け入れ基準:
  - EDGAR APIへのリクエストがUser-Agentヘッダーを付与して送信されることをテスト（モックHTTPサーバー等）で確認できる。
  - リクエストレートが10リクエスト/秒を超えないようレート制御されていることを、連続リクエストのタイミングを検証するテストで確認できる。
  - 取得したXBRLデータが正規化されたスキーマ（売上・利益・CF・自己資本等の共通フィールド）でキャッシュされ、同一銘柄への再取得時にキャッシュが利用されることをテストで確認できる。
  - `as_of`直前・同時刻・直後のfixtureにより、`filed_at <= as_of`だけがスクリーニングおよびバックテストへ入ることを検証できる。
  - 一時障害は各試行で10リクエスト/秒制限を守りながら合計3回までretryし、既定backoff後に成功する場合と上限到達時の例外伝播をfake clock/sleepで検証できる。設定・入力検証エラーはretryしない。

**FR-04 スクリーニング**
- 説明: ファンダメンタル品質フィルタ（黒字であること、財務健全性の基準を満たすこと等）と流動性フィルタにより、S&P500ユニバースから母集団を絞り込む（2段構えの第1段）。同じ`as_of`時点で利用可能なデータだけを使う。
- 受け入れ基準:
  - フィルタ条件を満たす銘柄のみが出力されることを、既知の入力データを用いたpytestで検証できる。
  - フィルタロジックが後述のプラガブルなインターフェース（NFR-07）に準拠し、フィルタの追加・組み合わせがコード変更なしに設定で行えることをテストで確認できる。

**FR-05 テクニカルシグナル**
- 説明: FR-04で絞り込んだ母集団に対し、トレンド系指標と押し目検出ロジックでエントリー候補を抽出する（2段構えの第2段）。初期実装のSMA・RSI（Wilder方式）・ATRはpandasで実装する。フィルタ・シグナルはプラガブルなインターフェースで実装し、設定でAND条件を明示して組み合わせること。候補は決定的な順位規則で最大10銘柄へ集約する。
- 受け入れ基準:
  - 少なくとも1つのトレンド系シグナルと1つの押し目検出シグナルが実装され、既知のOHLCVデータに対する出力をpytestで検証できる。
  - 新しいシグナルクラスを既存コード変更なしに追加し、既存シグナルと組み合わせて実行できることを統合テストで確認できる。
  - 同一入力・同一`as_of`・同一設定では候補の内容と順位が一致し、複数シグナルが同一銘柄の1つの候補へ集約されることをpytestで検証できる。

**FR-06 リスク管理チェック**
- 説明: 抽出されたエントリー候補に対し、ポジションサイズ計算、ストップ距離の算出、セクター集中度・銘柄間相関のチェックを行う。
- 受け入れ基準:
  - ポジションサイズとストップ距離が既知の入力（資金量・ボラティリティ等）から期待される値で算出されることをpytestで検証できる。
  - セクター集中や高相関銘柄の組み合わせが検出された場合に警告フラグが出力されることをテストで確認できる。
  - 口座評価額、想定エントリー価格、ATRベースのストップ価格が欠けている場合は、株数を推測せず明示的な「計算不能」結果を返すことをテストで確認できる。
  - 相関は候補と保有銘柄の日次リターンを取引日でinner joinして計算する。日付ずれ・重複日・必要本数未満・定数系列では誤った相関値を返さずdata-quality警告になることを検証できる。

**FR-07 テキスト収集**
- 説明: 銘柄別ニュース（Finnhub、60コール/分・ティッカー別）、新着8-K/10-Q（EDGAR）、経済指標カレンダー（FRED）を収集する。
- 受け入れ基準:
  - Finnhubへのリクエストが60コール/分の上限を超えないようレート制御されていることをテストで確認できる。
  - 各データソースの取得結果が共通のテキストデータスキーマで保存され、後続の定性分析（FR-08）の分析入力へ渡せることを統合テストで確認できる。

**FR-08 定性分析（スキル連携）**
- 説明: ニュース・開示テキストの解釈とスクリーニング結果の定性評価は、PythonからLLM APIを呼ばず、Claude Codeスキル（`.claude/skills/swing-daily`とその配下の専門家スキル）が担う。Python側の責務は次の3点に限定する。(1) 日次バッチのステップ6で、候補ごとの決定論的文脈（スコア内訳・リスク制約・過去判断）と収集済みの未信頼テキストを`analysis_input.json`として日付付きレポートディレクトリへ原子的に書き出す。(2) スキルが書いた`analysis_result.json`を`copilot-ingest-analysis`が読み、strictスキーマ・provenance・CON-03で機械検証する。(3) 検証を通った定性セクションだけでレポートを再描画する。スキルの出力は一切信頼せず、決定論的な数値（スコア・順位・株数・リスク判定）は分析側から書き換えられない。
- 受け入れ基準:
  - `analysis_input.json`・`analysis_result.json`が双方向ともpydanticのstrictスキーマ（`extra="forbid"`）で検証され、未知フィールド・スキーマ違反がエラーとして検出されることをpytestで確認できる。
  - 出力スキーマが「事実（`facts`）」と「解釈（`interpretation`）」を別フィールドとして強制し、`facts`の各要素が非空・非空白の`source_ids`を1件以上持つことを検証できる。
  - 引用された`source_ids`が、当該銘柄について`analysis_input.json`で実際に供給したID集合の部分集合であることを検証できる。部分集合でない銘柄はfail-closedで縮退表示となり、リトライしないことをテストで確認できる。
  - 書類種別・提出日・ソースURLといったコード側メタデータは`analysis_input.json`から解決され、スキル出力の申告値をそのまま採用しないことを確認できる。
  - facts、interpretation、risk/red flag、YoY変化、スクリーニング評価、verdict理由を含む全ユーザー表示文字列についてCON-03検査をingest時に一元実施し、違反銘柄が表示されず縮退することを検証できる。
  - `analysis_result.json`の`as_of`が`analysis_input.json`の`as_of`と一致しない場合はrun全体のhard failとし、部分的に読み込まないことを確認できる。
  - 銘柄ごとのverdict（`proceed`／`skip`）と、run全体の`no_trade`表示がレポートへ反映され、検証未通過・分析未実施の銘柄では「懸念なし」と誤読される表示が出ないことを確認できる。
  - `copilot-ingest-analysis`がネットワークアクセスもスクリーニング再計算も行わず、`report_context.json`に保存済みのブリーフの定性欄だけを差し替えることをテストで確認できる。
  - 通常のlive当日実行では、同一銘柄・戦略の過去の人間判断を最大3件まで構造化して`analysis_input.json`へ含められる。過去判断は現在の事実でも指示でもない旨を明記し、専用delimiter内でescapeする。
  - `--dry-run`、バックテスト、明示的な`--as-of`実行では過去判断を分析入力へ渡さず、先読みを防ぐことをテストで確認できる。

> **仕様変更（P7、スキル移行時）**: 旧LLM統合にあった`catalyst_quality`（触媒の質、表示専用）と`guidance_direction`（ガイダンス方向性）は新しい分析契約に含めず廃止した。いずれもランキング・リスク判定へ接続されていない表示専用フィールドであり、必要になれば`analysis/schemas.py`の任意フィールドとして復活できる。

**FR-09 レポート・通知**
- 説明: 日次結果をCLIへ見やすく表示し、同じ`DailyBrief`から監査用Markdownを生成する。DuckDBを正本とし、Markdownは再生成可能な派生成果物とする。Discord Webhook通知はオプション機能とする。
- 受け入れ基準:
  - `copilot-daily`が進捗ログをstderr、最終ブリーフをstdoutへ出し、市場概況・候補順位・シグナル・リスク・定性分析（未実施ならその旨）・警告を確認できる。`copilot-ingest-analysis`は同じ`DailyBrief`から同じレンダラで再描画する。
  - `reports/<run_date>/<run_id>.md`と`reports/latest.md`を同一ディレクトリの一時ファイルから原子的に置換し、同日再実行の履歴を上書きしない。
  - CLIとMarkdownが共通の`DailyBrief`を入力とし、表示形式ごとにデータ取得・判断ロジックを重複させない。
  - `notification.enabled: false`（デフォルト）の場合、Discord Webhookへの送信が行われないことをテストで確認できる。`notification.enabled: true`かつWebhook URLが設定されている場合のみ、モックWebhookエンドポイントに対してサマリ内容を含むペイロードが送信されることをテストで確認できる。
  - CLI・Markdown・通知内に断定的な売買指示（CON-03）が含まれないことをテストとレビューで確認できる。

**FR-10 バックテスト**
- 説明: 日次バッチと同じFilter/Signalロジックを使う決定的なポートフォリオシミュレータで戦略の履歴検証を行う。シグナルは当日終値確定後に生成し、約定は翌営業日以降に限る。財務データは提出日時点、価格データは評価日時点で利用可能な情報だけを使い、手数料・スリッページを織り込み、SPYをベンチマークとして比較する。終了日以前のユニバース履歴があればそれを使い、履歴が不足する場合は現在の構成銘柄へフォールバックして、生存者バイアスの限界をレポートに明記する。
- 受け入れ基準:
  - バックテスト実行コマンドで、手数料・スリッページを反映した損益曲線とS&P500ベンチマーク比較が出力されることを確認できる。
  - 同一の入力データ・戦略設定でバックテストを再実行した場合、同一の結果が再現されることをpytestで検証できる（再現性の確認）。
  - 手計算可能な1取引fixtureで、買いと売りの双方へ不利なスリッページと手数料が適用され、強制清算後の最終資産が厳密に一致することを検証できる。stopと最大保有日が同日に成立する場合はstopを優先する。
  - $0構成期間中のバックテストレポートに生存者バイアスの限界に関する注記が含まれることを確認できる。
  - バックテストの終了日より後のユニバース履歴を選ばず、履歴がない場合も現在構成銘柄で処理を継続して生存者バイアス注記を残すことを検証できる。
  - 将来の価格・提出前の財務情報を参照しないこと、ギャップを含むストップ約定規則、最大保有日数、同時保有上限、資金不足時の候補順位をfixtureで検証できる。

**FR-11 ペーパートレード記録**
- 説明: シグナル発生から人間の判断、仮想約定、損益までを記録し、対S&P500での成績を集計する。
- 受け入れ基準:
  - シグナル・判断・仮想約定・損益がDuckDBに記録され、記録後にレコードを再取得して整合性を確認できることをpytestで検証できる。
  - `copilot-decision`は`run_id`と候補銘柄を検証し、戦略が一意なら推定して判断を記録する。候補外の銘柄は拒否する。
  - 判断記録後、当該runの生成済みMarkdownの判断セクションをDuckDBから原子的に更新する。
  - 成績集計コマンドを実行すると、累積損益とS&P500騰落率との比較が出力されることを確認できる。

**FR-12 日次バッチ**
- 説明: FR-01〜FR-06を基盤とし、FR-07〜FR-09を含めた日次処理を、利用者が1日1回コマンドで手動実行することにより順次実行するオーケストレーション。FR-10（バックテスト）とFR-11（人間の判断記録）は同じストアとドメインロジックを使う別CLIとして扱う。各実行に一意な`run_id`を付与し、同じ評価対象日を再実行しても自然キーupsertにより重複データを生成しない。テキスト収集や分析入力エクスポートが失敗してもスクリーニングまでは完走するフェイルソフト設計とする。定性分析そのものは日次バッチの外（Claude Codeスキル）で実行し、`copilot-ingest-analysis`が別コマンドとして結果を取り込む。
- 受け入れ基準:
  - バッチ実行コマンドを同日中に2回実行した場合、2回目の実行が重複データを生成しないことをpytestで検証できる（冪等性の確認）。
  - 2回の実行が別々の`run_id`とステップ履歴を持ち、失敗した実行の後に再実行して正常完了できることをpytestで検証できる。
  - テキスト収集または分析入力エクスポートを意図的に失敗させるテストで、スクリーニング・シグナル抽出・レポート生成（定性分析部分を除く）までが正常完了し、runが`degraded`で終了コード0になることを確認できる（フェイルソフトの確認）。
  - ローカル実行時の所要時間が35分以内に収まることを、実行ログのタイムスタンプで確認できる。
  - 設定は外部I/Oとrun DB作成の前にstrictに検証され、未知キー・型の暗黙変換・非正の期間/本数/timeout・範囲外の比率/閾値・`sma_short >= sma_long`を明確な設定エラーとして拒否することを確認できる。
  - `--limit`は非負整数だけを受け付け、負数は外部I/Oとrun DB作成の前にusage error（終了コード2）で終了する。`0`は新規のユニバース候補を選ばず、既存の保有銘柄だけは安全上の監視対象として維持することを確認できる。

---

## 非機能要件

### 非機能要件一覧

| ID | 要件名 | 概要 |
|---|---|---|
| NFR-01 | コスト | LLM API従量課金なし。データ費のみで本番運用は月$25以内 |
| NFR-02 | 保守性 | 個人1人で保守可能。常時稼働サーバーなし、依存ライブラリ最小 |
| NFR-03 | 性能 | 日次バッチ合計35分以内（ローカル手動実行） |
| NFR-04 | 信頼性 | データ欠損検知、外部API障害時のフェイルソフトとリトライ |
| NFR-05 | 監査性 | 全シグナル・分析入出力・人間の判断を記録し後から検証可能 |
| NFR-06 | セキュリティ | APIキーはコードに含めない。リポジトリはプライベート |
| NFR-07 | 拡張性 | Strategy/Filter/Signal/DataProvider/Notifierをインターフェース分離 |
| NFR-08 | テスト品質 | line+branchカバレッジ95%以上、E2Eスモークテスト必須 |

### 各要件の説明と受け入れ基準

**NFR-01 コスト**
- 説明: 定性分析はClaude Codeスキルとして利用者のClaude Codeセッション内で実行され、本システムからLLM APIを呼ばないため、LLMのAPI従量課金は発生しない（既存のClaude Codeサブスクリプションの範囲内）。残る外部費用はデータ取得のみで、$0構成（yfinance・EDGAR・Finnhub無料枠・FRED）を維持する試作期は月$0、本番運用（P3〜）で有料データ基盤へ移行する場合も月$25以内に収める。
- 受け入れ基準: `src/`配下にLLM APIクライアント・APIキー・従量課金の会計処理が存在しないことをコード上で確認できる（`anthropic`パッケージへの依存を持たない）。有料データ基盤へ移行する場合の月額は契約プランの定額から事前に確認できる。

> **P7（スキル移行）での変更**: 旧NFR-01は`llm_calls`テーブルへの実績コスト記録と呼び出し前の月次予算ゲートを受け入れ基準としていた。LLM API呼び出し自体が無くなったため、予算会計・月次上限ゲート・実行単位の呼び出し上限・Batch API検討はいずれも廃止した。

**NFR-02 保守性**
- 説明: 常時稼働サーバーを持たず、依存ライブラリを最小限に保ち、個人1人が長期間保守できる構成とする。
- 受け入れ基準: システム構成に常時稼働のサーバープロセス（デーモン）が存在しないことをアーキテクチャ図・実行構成から確認できる。構造化データの永続化はDuckDB、株価時系列はParquetに限定し、同一目的のDBを重複導入しない。TA-Lib・backtesting.py・SQLite専用データアクセス層はP1〜P2の依存に含めない。

**NFR-03 性能**
- 説明: 日次バッチの合計実行時間を35分以内に収め、利用者が1日1回ローカルで手動実行した際に許容できる待ち時間に収める。
- 受け入れ基準: ローカル実行時のバッチ実行ログから、開始〜終了までの所要時間が35分以内であることを確認できる。

**NFR-04 信頼性**
- 説明: データ欠損を検知し、外部API障害時にはフェイルソフト（処理継続）とリトライを行う。
- 受け入れ基準: 主要データソース（yfinance/EODHD, EDGAR, Finnhub, FRED）のいずれかが意図的に失敗するモックを用いたテストで、欠損が検知されログ等に記録されることを確認できる。一時的な障害に対してリトライ処理が行われることをテストで確認できる。
- 通常のpytest suiteはautouseのsocket遮断により実ネットワーク接続を即時失敗させる。外部portにはfakeを注入し、live canaryは通常suiteと成功条件から分離する。

**NFR-05 監査性**
- 説明: 全実行・ステップ・候補・リスク評価・定性分析の入出力・人間の判断を記録し、特定の`run_id`のレポート入力を後から再構成可能な状態を保つ。
- 受け入れ基準: DuckDBのスキーマに、実行・ステップ・シグナル/候補・リスク評価・収集テキスト・人間の判断それぞれに対応するテーブルが存在する。`runs.config_hash`は検証済み`Settings`・選択した`StrategySpec`・`strategy_key`のcanonical JSON全体から算出する完全SHA-256指紋とし、同じ行の`metadata_json`にはprovider名、data tier、実効ユニバースのsnapshot日・identity、アプリ版・メタデータschema版を秘密情報なしで保存する。定性分析の監査証跡は`reports/<run_date>/<run_id>/`に残る`analysis_input.json`（分析へ渡した入力そのもの）・`analysis_result.json`（スキルの回答そのもの）・`report_context.json`（再描画に使ったブリーフのスナップショット）の3ファイルが担う。3文書は`run_id`、`as_of`、`strategy_key`、入力のcanonical JSONから計算した完全SHA-256 digestで束縛し、いずれかが不一致なら既存レポートを変更せずhard failする。任意の`run_id`のバッチ実行結果を、記録された入力参照と出力から再構成できることをテストで確認できる。

> **P7（スキル移行）での変更**: 旧NFR-05が定性分析の証跡としていた`llm_calls`テーブル（プロンプト全文・トークン数・コスト・レスポンスJSON）は、LLM API呼び出しの廃止に伴い削除した。証跡は上記のJSONアーティファクトへ置き換わっている。

**NFR-06 セキュリティ**
- 説明: APIキー等の秘匿情報をコードに含めず、ローカルの`.env`（`.gitignore`対象、python-dotenvで読み込み）で管理する。GitHubリポジトリを利用する場合（コード管理用、利用自体は任意）はプライベートリポジトリとする。
- 受け入れ基準: コード・設定ファイルにAPIキーがハードコードされていないことを、シークレットスキャン（例: `git grep`によるパターン検索）で確認できる。`.env`が`.gitignore`に含まれ、バージョン管理対象外であることを確認できる。GitHubリポジトリを利用する場合、プライベート設定であることを確認できる。

**NFR-07 拡張性**
- 説明: Strategy、Filter、Signal、DataProvider、Notifierをそれぞれ独立したインターフェースとして分離し、実装の追加・差し替えを容易にする。
- 受け入れ基準: 上記5種のインターフェース（抽象基底クラスまたはProtocol）がそれぞれ定義されており、各インターフェースに対して少なくとも1つの具体実装が存在することをコード上で確認できる。新しい実装を追加する際に既存の呼び出し側コードの変更が不要であることを、モック実装を追加するテストで確認できる。

**NFR-08 テスト品質**
- 説明: 「実際にアプリを動かしたときにバグがないレベル」の品質を、数値カバレッジとE2Eスモークテストの両輪で担保する。pytest-covによるline+branchカバレッジを全体で95%以上とし、`--cov-fail-under=95`を検証コマンドに組み込む。カバレッジ除外（`# pragma: no cover`）の使用は`if __name__ == "__main__":`ブロックとProtocol/ABCの抽象メソッド本体に限定し、テストのskip/xfailによるカバレッジ回避は禁止する。
- 受け入れ基準:
  - `uv run pytest`実行時、line+branchカバレッジが全体95%以上であり、`--cov-fail-under=95`のしきい値チェックを通過することを確認できる。
  - `# pragma: no cover`が付与されている箇所が`if __name__ == "__main__":`ブロックおよびProtocol/ABCの抽象メソッド本体のみであることを、コードレビューまたは`git grep "pragma: no cover"`等の確認手段で検証できる。
  - テストの`@pytest.mark.skip`/`@pytest.mark.xfail`がカバレッジ回避目的で使用されていないことを確認できる。
  - 外部API呼び出しを記録済みフィクスチャ/モックに差し替えた状態で`copilot-daily`相当のコマンドを一気通貫実行し、CLIブリーフとMarkdown生成まで正常終了（終了コード0）することをE2Eスモークテストとして検証できる。
  - `just verify`がruff、format check、mypy strict、line+branch coverage 95%以上のpytest、`mkdocs build --strict`、wheel smokeを非破壊で実行し、すべて終了コード0になることを確認できる。

---

## 制約

| ID | 制約内容 |
|---|---|
| CON-01 | 発注自動化はスコープ外（証券会社APIは使わない） |
| CON-02 | yfinanceは試作限定（非公式・個人利用のみ）。本番データ基盤にしない |
| CON-03 | 投資助言に該当する出力（断定的な売買指示）をCLI・Markdown・通知に含めない。最終判断は人間。検査はスキルへの指示だけに依存せず、`copilot-ingest-analysis`（`analysis/safety.py`の純関数）が全ユーザー表示テキストへ一元適用し、違反銘柄はfail-closedで縮退表示する |
| CON-04 | バックテスト好成績でも、3ヶ月以上のペーパートレードで対S&P500評価を経ずに実資金へ進まない |

---

## 用語集

| 用語 | 説明 |
|---|---|
| スイング〜ポジショントレード | 数日〜数ヶ月程度の期間ポジションを保有する株式売買スタイル |
| ユニバース | スクリーニング・分析の対象となる銘柄群（本システムではS&P500構成銘柄） |
| DataProvider | 株価データ取得元を抽象化するインターフェース。yfinance/EODHD等の実装を差し替え可能にする |
| XBRL | SEC EDGARが提供する財務データの構造化フォーマット |
| ファンダ品質フィルタ | 黒字・財務健全性などファンダメンタル指標に基づく銘柄の第1段絞り込み |
| プラガブル | インターフェースを介して実装を追加・差し替え・組み合わせできる設計方針 |
| 分析入力（`analysis_input.json`） | 日次バッチが書き出す、定性分析スキルへ渡す唯一の入力。決定論的な文脈ブロックと未信頼テキストを含む |
| 分析結果（`analysis_result.json`） | 定性分析スキルが書き出す回答。`copilot-ingest-analysis`が検証するまでレポートへ反映されない |
| provenance | 各factが引用する`source_ids`が、実際に供給した入力IDの部分集合であること。出典を辿れることの機械的保証 |
| fail-closed | 検証を通らなかった内容を、推測で補ったり再試行したりせず、非表示（縮退）にする方針 |
| verdict | 定性分析による銘柄ごとの推奨（`proceed`＝懸念なし／`skip`＝見送り推奨）。決定論的なスコア・順位は変更しない |
| フェイルソフト | 一部処理が失敗しても、可能な範囲まで処理を完走させる設計方針 |
| ペーパートレード | 実資金を用いず、仮想的な売買として記録・検証する取引 |
| 生存者バイアス | 上場廃止銘柄が除外されたデータでバックテストすることによる成績の過大評価 |
| 冪等性 | 同一処理を複数回実行しても結果が変わらない性質（本システムでは同日再実行への対応） |
